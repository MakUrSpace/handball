[package]
name = "handball-core"
version = "0.1.0"
edition = "2021"
description = "Core physics, court geometry, and rules engine for VR Handball"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
